services:
  sandbox:
    build:
      context: ./packages/sandbox-server
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - WORKSPACE_DIR=/workspace
      # - AUTH_TOKEN=my-secret-token
    volumes:
      - sandbox-workspace:/workspace

volumes:
  sandbox-workspace:
